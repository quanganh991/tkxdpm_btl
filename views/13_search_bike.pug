each each_bike, index in allBikeMatch
    h3= index
    p= '"name: " '+ each_bike.name
    p= '"type: " '+ each_bike.type
    p= '"weight: " '+ each_bike.weight
    p= '"licensePlate: " '+ each_bike.licensePlate
    p= '"manuafacturingDate: " '+ each_bike.manuafacturingDate
    p= '"producer: " '+ each_bike.producer
    p= '"cost: " '+ each_bike.cost
    p= '"idPark: " '+ each_bike.idPark
    p= each_bike.type.toString() == "ebike" ? '"batteryPercentage: " '+ each_bike.batteryPercentage : ""
    p= each_bike.type.toString() == "ebike" ? '"loadCycles: " ' + each_bike.loadCycles : ""
    p= each_bike.type.toString() == "ebike" ? '"timeRemaining: " ' + each_bike.timeRemaining : ""
    br
